name: Build and Deploy ReactFlux to GitHub Pages

on:
  push:
    branches:
      - main  # Deploy when pushing to main branch
  schedule:
    - cron: '28 3 * * *'  # Daily rebuild at 3:28 AM UTC
  workflow_dispatch:  # Allow manual trigger

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout ReactFlux repository
        uses: actions/checkout@v4
        with:
          repository: electh/ReactFlux
          ref: main
          path: reactflux
      
      - name: Checkout your repository for _pages folder
        uses: actions/checkout@v4
        with:
          path: my-repo
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: Install pnpm
        run: npm install -g pnpm
      
      - name: Install dependencies
        working-directory: ./reactflux
        run: pnpm install --frozen-lockfile
      
      - name: Build ReactFlux
        working-directory: ./reactflux
        run: pnpm run build
        env:
          NODE_ENV: production
      
      - name: Copy custom files from _pages folder
        run: |
          if [ -d "my-repo/_pages" ]; then
            echo "Copying files from _pages to build directory..."
            cp -R my-repo/_pages/. reactflux/build/
            echo "Files copied successfully!"
          else
            echo "No _pages folder found, skipping..."
          fi
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./reactflux/build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
